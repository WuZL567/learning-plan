/**
 * 1.5 隐式类型转换 - 6道输出题
 *
 * 验收标准：6道题输出全部正确，且每道题都写清楚推导过程（用你自己的话，一步一步推）
 *
 * 文件路径：src/module-01-javascript/1.5-隐式类型转换/quizzes.js
 * 笔记路径：notes/module-01-javascript/1.5-隐式类型转换.md
 *
 * 做完后告诉我文件路径，我会来验收。
 */

// ============================================================
// 第1题：字符串 + 混合类型
// 请写出下面每一行的输出，并用注释说明推导过程
// ============================================================
console.log(1 + '2' + 3);
// 你的答案（输出 + 推导过程）：
// "123"，+符号有字符串就全部转成字符串，没有字符串则全部转成数字，所以： 1 + '2' 转成 '1' + '2'，变成 '12' + 3，再转成 '12' + '3' 得 '123'；

console.log(1 + 2 + '3');
// 你的答案（输出 + 推导过程）：
// "33"，+符号有字符串就全部转成字符串，没有字符串则全部转成数字，所以：1 + 2 得 3，然后 3 + '3' 转成 '3' + '3' 得 "33"；

console.log('1' + 2 + 3);
// 你的答案（输出 + 推导过程）：
// "123"，+符号有字符串就全部转成字符串，没有字符串则全部转成数字，所以：'1' + 2 转成 '1' + '2' 得 '12'，然后 '12' + 3 转成 '12' + '3' 得 '123'；

// ============================================================
// 第2题：null / undefined 在运算中的行为
// 请写出下面每一行的输出，并用注释说明推导过程
// ============================================================
console.log(null + 1);
// 你的答案（输出 + 推导过程）：
// 1，+符号有字符串就全部转成字符串，没有字符串则全部转成数字，所以：null + 1 转成 0 + 1 得 1；

console.log(undefined + 1);
// 你的答案（输出 + 推导过程）：
// NaN，+符号有字符串就全部转成字符串，没有字符串则全部转成数字，所以：undefined + 1 转成 NaN + 1 得 NaN；

console.log(null == 0);
// 你的答案（输出 + 推导过程）：
// false，宽松比较：同类型直接===，null 和 undefined 互相等，字符串vs数字转数字，有布尔值转数字，对象vs非对象拆对象，对象执行valueOf和toString，Date对象则先执行toString
// null 只和 undefined 在 == 时为true；

console.log(null == undefined);
// 你的答案（输出 + 推导过程）：
// true，null 只和 undefined 在 == 时为true；

console.log(null === undefined);
// 你的答案（输出 + 推导过程）：
// false，===符号是严格比较，需要对比值和类型，首先null和undefined就是两个不同的基本类型，所以 null === undefined 得 false；

// ============================================================
// 第3题：数组和对象参与类型转换
// 请写出下面每一行的输出，并用注释说明推导过程
// ============================================================
console.log([] + []);
// 你的答案（输出 + 推导过程）：
// ''，对象需要拆开，对象先执行valueOf函数，返回自身则继续调用toString函数，[].toString返回''，所以 [] + [] 转成 '' + '' 得 ''；

console.log([] + {});
// 你的答案（输出 + 推导过程）：
// "[object Object]"，对象需要拆开，对象先执行valueOf函数，返回自身则继续调用toString函数，[].toString返回''，{}.toString返回'[object, Object]'，
// 所以 [] + {} 转成 '' + '[object Object]' 得 '[object Object]'；

console.log({} + []);
// 你的答案（输出 + 推导过程）：
// "[object Object]"，同上所以 {} + [] 转成 '[object Object]' + '' 得 '[object Object]'；

console.log({} + {});
// 你的答案（输出 + 推导过程）：
// "[object Object][object Object]"，同上所以 {} + {} 转成 '[object Object]' + '[object Object]' 得 '[object Object][object Object]'；

// ============================================================
// 第4题：== 操作符的经典陷阱
// 请写出下面每一行的输出，并用注释说明推导过程
// ============================================================
console.log([] == 0);
// 你的答案（输出 + 推导过程）：
// true，==符号是宽松比较，类型相同直接===，null只和undefined相同，字符串vs数字转数字，有布尔值转数字，对象vs非对象，先拆分对象，先执行valueOf函数，如果返回自身则继续用toString函数来获取，但是Date对象先执行toString函数；
// 所以[].toString得 ''，'' 转成 0，所以 0 == 0 得 true；

console.log([] == false);
// 你的答案（输出 + 推导过程）：
// true，==符号是宽松比较，类型相同直接===，null只和undefined相同，字符串vs数字转数字，有布尔值转数字，对象vs非对象，先拆分对象，先执行valueOf函数，如果返回自身则继续用toString函数来获取，但是Date对象先执行toString函数；
// 所以[].toString得 ''，'' 转成 0，false 转成 0，所以 0 == 0 得 true；

console.log([] == ![]);
// 你的答案（输出 + 推导过程）：
// true，==符号是宽松比较，类型相同直接===，null只和undefined相同，字符串vs数字转数字，有布尔值转数字，对象vs非对象，先拆分对象，先执行valueOf函数，如果返回自身则继续用toString函数来获取，但是Date对象先执行toString函数；
// 所以[].toString得 ''，'' 转成 0，![]转成 false，再转成 0，所以 0 == 0 得 true；

console.log('' == false);
// 你的答案（输出 + 推导过程）：
// true，==符号是宽松比较，类型相同直接===，null只和undefined相同，字符串vs数字转数字，有布尔值转数字，对象vs非对象，先拆分对象，先执行valueOf函数，如果返回自身则继续用toString函数来获取，但是Date对象先执行toString函数；
// 所以 '' 转成数字 0，false转成数字 0，所以 0 == 0 得 true；

// ============================================================
// 第5题：非 + 算术运算符和关系运算符
// 请写出下面每一行的输出，并用注释说明推导过程
// ============================================================
console.log('6' - 2);
// 你的答案（输出 + 推导过程）：
// 4，非+和非==运算时，统统转成数字，所以 '6' - 2 转成 6 - 2 得 4；

console.log('6' * '2');
// 你的答案（输出 + 推导过程）：
// 12，非+和非==运算时，统统转成数字，所以 '6' * '2' 转成 6 * 2 得 12；

console.log('hello' - 1);
// 你的答案（输出 + 推导过程）：
// NaN，非+和非==运算时，统统转成数字，所以 'hello' 转数字时是 NaN，所以 NaN - 1 得 NaN；

console.log('5' > 3);
// 你的答案（输出 + 推导过程）：
// true，非+和非==运算时，统统转成数字，所以 '5' > 3 转成 5 > 3 得 true；

console.log('a' < 'b');
// 你的答案（输出 + 推导过程）：
// true，按照Unicode位置比较；

// ============================================================
// 第6题：ToPrimitive 与 valueOf / toString
// 请写出下面每一行的输出，并用注释说明推导过程
// 提示：对象转原始值时会先调 valueOf() 再调 toString()，
// 日期对象相反，先 toString() 再 valueOf()
// ============================================================
console.log(new Date() + 1);
// 你的答案（输出 + 推导过程）：
// "Mon Aug 10 2026 14:31:37 GMT+0800（中国标准时间）1"，
// 先获取日期对象toString()和valueOf()的结果，得到字符串，再通过字符串和数字1使用+符号拼接而成，因为字符串和数字之间的加号就是拼接作用；

const obj = {
  valueOf() { return 10; },
  toString() { return 'hello'; }
};
console.log(obj + 1);
// 你的答案（输出 + 推导过程）：
// 11，拆分对象时，先执行对象的valueOf函数，没有返回自身，则使用 10，所以是 10 + 1 得 11；

console.log(obj + '1');
// 你的答案（输出 + 推导过程）：
// "101"，拆分对象时，先执行对象的valueOf函数，没有返回自身，则使用 10，所以是 10 + '1' 得 '101'；
